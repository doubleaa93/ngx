import { OnInit, OnDestroy, ComponentFactoryResolver, OnChanges, SimpleChanges } from '@angular/core';
import { DeReCrudProviderService } from '../../providers/provider/provider.service';
import { IField, IFieldReference } from '../models/schema';
import { FormGroup, AbstractControl } from '@angular/forms';
import { FormState } from '../models/form-state';
import { FormStateService } from '../services/form-state.service';
import { ComponentHostDirective } from './component-host.directive';
export declare class InputFieldHostComponent implements OnInit, OnChanges, OnDestroy {
    private stateService;
    private componentFactoryResolver;
    private providerService;
    private _componentRefs;
    private _submissionErrorsChangeSubscription;
    private _formChangeSubscription;
    private _valueOnFocus;
    componentHost: ComponentHostDirective;
    formId: number;
    form: FormGroup;
    struct: string;
    block: string;
    field: IField;
    parentForm: AbstractControl;
    parentPath: string;
    state: FormState;
    fieldReference: IFieldReference;
    constructor(stateService: FormStateService, componentFactoryResolver: ComponentFactoryResolver, providerService: DeReCrudProviderService);
    readonly formPath: string;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    destroyRefs(): void;
    shouldRender(): boolean;
    render(): void;
    updateInputs(): void;
    onFocus: () => void;
    onBlur: () => void;
    onChange: (e: any) => void;
    private mapType;
}
