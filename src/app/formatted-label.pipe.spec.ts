import { FormattedLabelPipe } from './formatted-label.pipe';

describe('FormattedLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
